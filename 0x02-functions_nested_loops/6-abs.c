#include "main.h"

/**
 * _abs -  computed the absolute value of an integer
 * @r: the integer to be computed
 * Return: the absolute value
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
